describe('Url history service', function() {

  beforeEach(module('exposure'));

  beforeEach(inject(function() {

  }));

  it('loads the parameter url in the iframe');
  it('sets the iframe to the parameter width and height');
  it('toggles orientation when portait mode is changed');


  it('displays a reason if loading the url fails');
  it('loads new pages if the url changes');
  it('reloads the current page');
});